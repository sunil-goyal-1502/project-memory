{
  "description": "Project memory - captures decisions and loads context. Hooks are registered in ~/.claude/settings.json with absolute paths to avoid CLAUDE_PLUGIN_ROOT resolution issues.",
  "hooks": {}
}
